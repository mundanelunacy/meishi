# Shared defaults loaded by Vite in every mode.
VITE_GOOGLE_AUTH_MODE=mock

# Set this for real Google OAuth. Leave blank in local development if you want
# Meishi to fall back to explicit mock auth.
VITE_GOOGLE_CLIENT_ID=
